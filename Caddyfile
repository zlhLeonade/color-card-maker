# 替换为你的实际域名或 NAS IP
# 有域名（自动 HTTPS）：
# color.lihzh0124.xyz {
#   root * /srv
#   file_server
#   try_files {path} /index.html
# }

# 内网 IP 访问（无 HTTPS）：
:80 {
  root * /srv
  file_server
  try_files {path} /index.html
}
